apiVersion: 1

contactPoints:
  - orgId: 1
    name: slack-alerts
    receivers:
      - uid: slack-webhook
        type: slack
        settings:
          url: $__env{GF_SLACK_WEBHOOK_URL}
        disableResolveMessage: false
