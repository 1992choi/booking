{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": ".claude/hooks/block-git-write.sh"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(./gradlew *)",
      "Bash(git status)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); ctx=$(echo \"$input\" | jq -r '.context_window.used_percentage // empty'); h5=$(echo \"$input\" | jq -r '.rate_limits.five_hour.used_percentage // empty'); h5r=$(echo \"$input\" | jq -r '.rate_limits.five_hour.resets_at // empty'); d7=$(echo \"$input\" | jq -r '.rate_limits.seven_day.used_percentage // empty'); d7r=$(echo \"$input\" | jq -r '.rate_limits.seven_day.resets_at // empty'); fr() { date -r \"$1\" '+%m/%d %H:%M' 2>/dev/null || date -d \"@$1\" '+%m/%d %H:%M' 2>/dev/null; }; out=\"\"; [ -n \"$ctx\" ] && out=$(printf 'ctx = %.0f%%' \"$ctx\"); if [ -n \"$h5\" ]; then p=$(printf '5h = %.0f%%' \"$h5\"); [ -n \"$h5r\" ] && p=\"$p (reset $(fr \"$h5r\"))\"; out=\"${out:+$out | }$p\"; fi; if [ -n \"$d7\" ]; then p=$(printf '7d = %.0f%%' \"$d7\"); [ -n \"$d7r\" ] && p=\"$p (reset $(fr \"$d7r\"))\"; out=\"${out:+$out | }$p\"; fi; [ -n \"$out\" ] && echo \"[ $out ]\""
  }
}
