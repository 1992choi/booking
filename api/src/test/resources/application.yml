spring:
  kafka:
    bootstrap-servers: localhost:9092
  datasource:
    url: jdbc:mysql://localhost:3306/db_api?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  mongodb:
    uri: mongodb://localhost:27017/db_api_audit_test

booking:
  jwt:
    secret: dev-secret-must-be-at-least-32-bytes-long-for-hmac-sha256
    access-token-ttl: PT1H
    refresh-token-ttl: P30D
  notification:
    url: http://localhost:8083
